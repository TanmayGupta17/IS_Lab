str = "the house is being sold tonight"
key = chr(ord('a')+7-1)

newkey = key+str
res = []
shifted = []
count1 = 0
count2 = 0
for i in str:
    if i.isalpha():
        if count1 < len(key):
            res.append(key[count1])
            count+=1
            shifted.append(str[i])
        elif
newkey = newkey[:len(str)]
print(newkey)

cipher = ""
print(cipher)
# newKey = t+str
# print(newKey)
